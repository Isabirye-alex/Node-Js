<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Order Confirmation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f6f6f6;
            color: #333;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 650px;
            margin: auto;
            background: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 1px 4px rgba(0, 0, 0, 0.1);
        }

        .header {
            background: #4CAF50;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 8px 8px 0 0;
        }

        .order-summary {
            padding: 20px 0;
        }

        .order-summary h3 {
            margin-bottom: 10px;
        }

        .order-item {
            display: flex;
            margin-bottom: 15px;
            border-bottom: 1px solid #eee;
            padding-bottom: 10px;
        }

        .order-item img {
            width: 60px;
            margin-right: 15px;
            border-radius: 5px;
        }

        .item-details {
            flex: 1;
        }

        .info {
            margin: 10px 0;
        }

        .cta-button {
            display: inline-block;
            background: #4CAF50;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            margin-top: 20px;
        }

        .footer {
            margin-top: 30px;
            font-size: 13px;
            text-align: center;
            color: #999;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h2>Hello, {{receiverName}}!, Your order has been received</h2>
        </div>

        <div class="order-summary">
            <h3>Order Details</h3>
            <p class="info"><strong>Order ID:</strong> {{orderId}}</p>
            <p class="info"><strong>Order Date:</strong> {{orderDate}}</p>
            <p class="info"><strong>Payment Method:</strong> {{paymentMethod}}</p>
            <p class="info"><strong>Shipping Address:</strong><br /> {{shippingAddress}}</p>

            <h3>Items</h3>
            {{#each orderItems}}
            <div class="order-item">
                <img src="{{this.image}}" alt="{{this.name}}" />
                <div class="item-details">
                    <strong>{{this.name}}</strong><br />
                    Quantity: {{this.quantity}}<br />
                    Price: UGX{{this.price}}
                </div>
            </div>
            {{/each}}

            <p class="info"><strong>Total:</strong> UGX{{total}}</p>

            <a href="{{website}}" class="cta-button">Shop More</a>
        </div>

        <div class="footer">
                    <table width="100%" cellpadding="0" cellspacing="0" border="0" style="background-color: #f4f4f4; padding: 20px;">
                        <tr>
                            <td align="center" style="font-size: 14px; color: #444444; font-family: Arial, sans-serif;">
                                <p style="margin: 0;">&copy; {{year}} {{company}}. All rights reserved.</p>
                                <p style="margin: 5px 0;">
                                    <a href="#" style="color: #4a90e2; text-decoration: none;">Privacy Policy</a> |
                                    <a href="#" style="color: #4a90e2; text-decoration: none;">Terms of Service</a>
                                </p>
                                <p style="margin: 5px 0;">Need help? <a href="mailto:{{supportEmail}}"
                                        style="color: #4a90e2;">{{supportEmail}}</a></p>
                            </td>
                        </tr>
                    </table>

        </div>
    </div>
</body>

</html>